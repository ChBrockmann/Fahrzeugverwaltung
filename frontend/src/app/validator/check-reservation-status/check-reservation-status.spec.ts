import { CheckReservationStatus } from './check-reservation-status';

describe('CheckReservationStatus', () => {
  it('should create an instance', () => {
    expect(new CheckReservationStatus()).toBeTruthy();
  });
});
